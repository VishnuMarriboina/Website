apiVersion: v1
kind: Service
metadata:
  name: grpc-web-proxy
  labels:
    app: grpc-web-proxy
spec:
  type: LoadBalancer
  selector:
    app: grpc-web-proxy
  ports:
    - name: grpc-web
      port: 8080
      targetPort: 8080
      protocol: TCP
